import React from "react";
import useTitle from "../../hooks/useTitle";

const Login = () => {
  useTitle("Login");
  return (
    <div>
      <h3>Login</h3>
    </div>
  );
};

export default Login;
